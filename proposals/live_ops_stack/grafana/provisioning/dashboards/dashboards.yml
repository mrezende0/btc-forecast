apiVersion: 1

providers:
  - name: btc-forecast
    orgId: 1
    folder: btc-forecast
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
# Dashboards JSON ficam em ./grafana/provisioning/dashboards/*.json
# TODO: criar overview.json (predict_latency p95, signal_count_7d, psi_max,
# open_positions, pnl_paper_cum, api_errors_rate, killed_flag).
